# 🔄 HyperOS Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────┐
│                        USER DESKTOP                          │
│  ┌────────────┐  ┌──────────┐  ┌─────────┐  ┌──────────┐  │
│  │  Notepad   │  │  Chrome  │  │  Files  │  │   Apps   │  │
│  └────────────┘  └──────────┘  └─────────┘  └──────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │         HyperOS Chat Overlay (Always On Top)         │  │
│  │  ┌────────────────────────────────────────────────┐  │  │
│  │  │ User: Open Notepad and type Hello              │  │  │
│  │  │ HyperOS: Executing task...                     │  │  │
│  │  │ HyperOS: Task completed successfully!          │  │  │
│  │  └────────────────────────────────────────────────┘  │  │
│  │  [Input field: Type your command here...        ]  │  │
│  │  [Send Button]                                      │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## Agent Loop Flow

```
┌──────────────────────────────────────────────────────────────┐
│                    HYPEROS AGENT LOOP                         │
└──────────────────────────────────────────────────────────────┘

User Input: "Open Notepad and type Hello World"
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│ STEP 1: ANALYZE                                              │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ screen_capture.py                                        │ │
│ │ • Captures full desktop screenshot                       │ │
│ │ • Returns PIL Image object                               │ │
│ └─────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ element_detector.py                                      │ │
│ │ • Runs Tesseract OCR on screenshot                       │ │
│ │ • Detects text elements and coordinates                  │ │
│ │ • Returns: [{"text": "Start", "x": 20, "y": 800}]       │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│ STEP 2: PLAN                                                 │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ mistral_api.py                                           │ │
│ │ • Converts screenshot to base64                          │ │
│ │ • Sends to Mistral Pixtral API                          │ │
│ │ • Includes task + screenshot + detected elements         │ │
│ │                                                           │ │
│ │ ┌─────────────────────────────────────────────────────┐ │ │
│ │ │  MISTRAL PIXTRAL (Cloud API)                        │ │ │
│ │ │  • Analyzes screenshot with vision model            │ │ │
│ │ │  • Understands desktop state                        │ │ │
│ │ │  • Decides next action to take                      │ │ │
│ │ │  • Returns JSON response                            │ │ │
│ │ └─────────────────────────────────────────────────────┘ │ │
│ │                                                           │ │
│ │ Response:                                                 │ │
│ │ {                                                         │ │
│ │   "reasoning": "I see the desktop. Need to click Start", │ │
│ │   "action": {                                             │ │
│ │     "type": "click",                                      │ │
│ │     "x": 20,                                              │ │
│ │     "y": 800,                                             │ │
│ │     "target": "Start button"                              │ │
│ │   }                                                       │ │
│ │ }                                                         │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│ STEP 3: EXECUTE                                              │
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ action_executor.py                                       │ │
│ │ • Receives action from Mistral                           │ │
│ │ • Executes using PyAutoGUI                               │ │
│ │                                                           │ │
│ │ pyautogui.click(20, 800)  ← REAL MOUSE CLICK!           │ │
│ │                                                           │ │
│ │ • Mouse moves to (20, 800)                               │ │
│ │ • Click happens on actual desktop                        │ │
│ │ • Returns execution result                               │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────┐
│ STEP 4: VERIFY & LOOP                                        │
│ • Wait 1.5 seconds for UI to update                          │
│ • Add action to conversation history                         │
│ • Check if task complete                                     │
│ • If not complete, go back to STEP 1                         │
│ • Max 50 iterations                                          │
└─────────────────────────────────────────────────────────────┘
    │
    ▼
Task Complete! ✅
```

## File Interactions

```
main.py
  │
  ├─→ agent.py (HyperOSAgent)
  │     │
  │     ├─→ screen_capture.py
  │     │     └─→ Returns: PIL Image
  │     │
  │     ├─→ element_detector.py
  │     │     └─→ Returns: List of UI elements
  │     │
  │     ├─→ mistral_api.py
  │     │     │
  │     │     ├─→ config.py (API key)
  │     │     │
  │     │     └─→ Mistral API (Cloud)
  │     │           └─→ Returns: Action JSON
  │     │
  │     └─→ action_executor.py
  │           └─→ PyAutoGUI → Desktop
  │
  └─→ ui_overlay.py (Tkinter UI)
        └─→ Calls agent.execute_task()
```

## Data Flow

```
User Command
    ↓
[agent.py] → execute_task()
    ↓
[screen_capture.py] → Screenshot (PIL Image)
    ↓
[element_detector.py] → OCR Elements (List)
    ↓
[mistral_api.py] → Encode to base64
    ↓
[Mistral API] → Vision Analysis
    ↓
[mistral_api.py] → Parse JSON Response
    ↓
[action_executor.py] → PyAutoGUI Action
    ↓
[Desktop] → REAL AUTOMATION!
    ↓
[agent.py] → Check if complete
    ↓
Loop or Return Result
```

## Component Responsibilities

| Component | Responsibility | Technology |
|-----------|---------------|------------|
| `main.py` | Entry point, initialization | Python |
| `agent.py` | Core loop orchestration | Python |
| `screen_capture.py` | Desktop screenshots | PIL/Pillow |
| `element_detector.py` | OCR text detection | Tesseract |
| `mistral_api.py` | AI vision analysis | Mistral Pixtral API |
| `action_executor.py` | Desktop automation | PyAutoGUI |
| `ui_overlay.py` | User interface | Tkinter |
| `config.py` | Configuration | Python |

## Key Differences from Claude Cowork

```
Claude Cowork              HyperOS
─────────────────         ─────────────────
Anthropic API       →     Mistral AI API
Claude 3.5 Sonnet   →     Pixtral 12B
Electron UI         →     Tkinter UI
Higher cost         →     Lower cost
Same automation     =     Same automation (PyAutoGUI)
Same architecture   =     Same architecture (Analyze-Plan-Execute)
```

## Safety Mechanisms

```
┌─────────────────────────────────────────┐
│ Safety Layer                             │
├─────────────────────────────────────────┤
│ • PyAutoGUI Failsafe (mouse to corner)  │
│ • Max 50 steps per task                  │
│ • 0.5s pause between actions             │
│ • 1.5s wait after each action            │
│ • Error handling on API failures         │
│ • Conversation history (last 5 steps)    │
└─────────────────────────────────────────┘
```

## Performance Metrics

```
Average Action Time:
├─ Screenshot capture:     ~0.1s
├─ OCR detection:          ~0.5s
├─ Mistral API call:       ~2-3s
├─ Action execution:       ~0.5s
├─ Wait time:              ~1.5s
└─ Total per cycle:        ~5s

API Costs per Action:
└─ Mistral Pixtral:        ~$0.0003
```

---

This architecture ensures:
✅ Real desktop automation
✅ Vision-based understanding
✅ Reliable action execution
✅ Cost-effective operation
✅ Safe and controlled execution
